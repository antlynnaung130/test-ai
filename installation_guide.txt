================================================================================
                    BORG AI ROBOT 2026 - INSTALLATION GUIDE
                                ULTIMATE DESTROYER
================================================================================

📅 Version: 2026.0
🔢 Build: 2026.001

================================================================================
                        SYSTEM REQUIREMENTS
================================================================================

🔹 Operating System:
   - Linux (Ubuntu, Debian, CentOS, Kali, etc.)
   - macOS (10.15+)
   - Windows (10/11 with WSL or Command Prompt)

🔹 Python Version:
   - Python 3.6 or higher (Recommended: Python 3.8+)

🔹 Required Space:
   - Minimum: 50 MB

🔹 Network:
   - Internet connection required

================================================================================
                        STEP 1: INSTALL PYTHON
================================================================================

📌 For Linux (Ubuntu/Debian):
   sudo apt update
   sudo apt install python3 python3-pip -y

📌 For Linux (CentOS/RHEL):
   sudo yum install python3 python3-pip -y

📌 For macOS:
   /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
   brew install python3

📌 For Windows:
   # Download from: https://www.python.org/downloads/
   # Check "Add Python to PATH" during installation

# Verify Python Installation
python3 --version
pip3 --version

================================================================================
                        STEP 2: DOWNLOAD & INSTALL
================================================================================

# Clone the repository
git clone https://github.com/antlynnaung130/test-ai.git

# Enter the project directory
cd test-ai

# Install dependencies
python -m pip install -r requirements.txt

# Run the tool
python url_scanner.py

================================================================================
                        TROUBLESHOOTING
================================================================================

❌ Problem: 'git' command not found
   Solution: 
   - Linux: sudo apt install git
   - macOS: brew install git
   - Windows: Download from https://git-scm.com/

❌ Problem: 'python' command not found
   Solution:
   - Use 'python3' instead of 'python'
   - OR: Check Python installation

❌ Problem: ModuleNotFoundError
   Solution:
   python -m pip install aiohttp colorama

================================================================================
                        DISCLAIMER
================================================================================

⚠️  WARNING: This tool is for EDUCATIONAL and AUTHORIZED testing purposes ONLY!
⚠️  Unauthorized use against systems you do not own is ILLEGAL and UNETHICAL!

================================================================================
                    © 2026 Borg AI Collective - All Rights Reserved
================================================================================
